---
name: 3d-art-direction
description: Art-direction discipline for AI-driven 3D work in Blender and Unreal Engine — writing executable specs, matching a reference or concept image, and controlling final output quality. 用于 Blender / UE 的建模、材质、灯光、布料模拟、毛发绒面与场景真实感优化。Use this skill whenever the user asks to build, texture, light, simulate, or optimize anything in Blender or Unreal Engine; wants to match a design reference, concept art, or effect image; asks for a modeling, lighting, or material plan, spec, or execution document; mentions Blender MCP, UE MCP, or driving a DCC tool through an agent; or is debugging why a render "looks CG", "looks fake", "looks flat", or "doesn't match the reference". Apply it even when the request sounds simple — "make a cushion", "add fur to this carpet", "fix the lighting" — because the failure modes this skill prevents are not visible in the request itself.
---

# 3D Art Direction

一套用于**通过 AI 驱动 DCC 工具**（Blender、Unreal Engine 等）产出高质量 3D 内容的工作纪律。

它解决的不是"怎么调用 API"——那部分模型本来就会。它解决的是**判断力问题**：哪个参数真正决定成败、怎么在看不准渲染图的情况下验证结果、参考图自相矛盾时听谁的。

---

## 为什么需要这套纪律

AI 驱动 3D 制作有一个结构性弱点：**模型对自己产出的画面判断力很弱。**

问"这样够蓬松吗"，答案不可靠；问"透红了没有"，答案可靠。因此整套方法的核心是——**把主观的艺术判断，尽可能转译成可以用数字或二值判定的检查点。**

这不是形式主义。在无法目视验收的前提下，数值门槛是唯一可靠的反馈通道。

---

## 五条核心原则

### 1. 诊断根因，不要修症状

先问"为什么现在看起来不对"，再动手。表层现象和根因往往隔着好几层。

| 症状 | 常见的错误归因 | 实际根因 |
|---|---|---|
| 嵌墙物体像贴纸 | 深度不够 | 内腔没有明暗梯度 |
| 床没有悬浮感 | 缺灯带 | 缺让灯带藏进去的台沿几何 |
| 绒面像磨砂塑料 | shader 参数不对 | 场景缺掠射角光源，Sheen 没被激发 |
| 地毯像草坪 | 毛发密度不够 | 没有做簇状收拢 |
| 加了灯画面反而变暗 | 灯不够亮 | 自动曝光在反向补偿 |

**写方案时把诊断写在最前面。** 执行者知道"为什么"，才不会在遇到阻力时自作主张走回头路。

### 2. 找出权重最高的那一个参数

每个任务通常有一个参数，它的影响超过其余所有参数之和。识别它、标注它、把调参预算集中在它身上。

| 任务 | 权重最高的参数 |
|---|---|
| 抱枕/床品蓬松度 | `bending_stiffness`（褶皱性格的总开关） |
| 长绒地毯 | `clump_factor`（成簇 vs 草坪） |
| 场景照明 | 曝光是否先锁定（决定其余所有强度值是否有意义） |
| 嵌入感 | 内腔材质是否独立 + 明暗渐变方向 |
| "CG 感" | 硬边有没有 2～3mm 倒角 |

列二十个参数不如把这一个讲透：它控制什么、允许区间、调错会呈现什么症状。

### 3. 用数字门槛替代目测

每个阶段结束必须有一个**可以用数字或是/否回答**的验收项。

反例：「确认地毯看起来足够蓬松」
正例：「把底布改成纯红，从低视角和俯视各渲一张，**任何位置透红即不合格**」

常用门槛见 `references/gates.md`。设计新门槛时的判据是：**换一个模型来执行，会不会给出同样的结论？**

### 4. 顺序即正确性

很多任务里，步骤顺序本身就是对错问题，不是效率问题。

- **锁曝光 → 再调灯光强度。** 反了会陷入"加灯变暗→再加灯"的循环
- **布料模拟 → 再做滚边。** 反了滚边会被一起解算成面条
- **设原点 → 再 apply transform（且 `location=False`）。** 反了原点被重置
- **建顶点组 → 再挤出/布尔。** 顶点组是模拟后唯一可靠的几何锚点
- **结构定案 → 再做软体资产。** 光和结构没定，布料褶皱深浅无法判断

写方案时，凡是顺序有讲究的地方**都要写明理由**，否则执行者会为了省事重排。

### 5. 参考图权重分层

参考图（尤其 AI 生成的）内部常不自洽。**开工前先定权重，冲突时按序裁决。**

默认优先级：

1. **透视图 / 主视角** — 定整体轮廓、比例、氛围
2. **尺寸表 / 标注** — 通常可信，直接采用
3. **正视图** — 定比例关系
4. **侧视图 / 次要视图** — 仅作参考，不严格对齐

并且明确告知执行者：**不要追求 100% 复刻。** 抓住 3～5 条"目标特征"即可，剩余差异属媒介差异。照着不自洽的三视图建模会陷入死循环。

---

## 标准工作流

```
1. 勘察   → 报告环境、版本、现有资产、可用能力。不假设，先问。
2. 诊断   → 指出根因；定义 3~5 条"目标特征"作为全程判据
3. 定序   → 排出阶段顺序，标注哪些顺序不可调换及原因
4. 写规格 → 用 assets/spec-template.md 的六段式结构
5. 分阶段执行 → 每阶段停下报验收数值，等确认再继续
6. 收尾   → 交付清单 + 症状对照表
```

**第 5 步的"停下来"是刻意的。** 一口气跑完全流程，出问题时无法定位是哪一步引入的。

---

## 输出规格的六段式结构

任何交给执行者的方案都用这个结构。模板见 `assets/spec-template.md`。

| 段落 | 内容 | 为什么需要 |
|---|---|---|
| **0. 范围与诊断** | 根因、目标特征、**明确排除什么** | 防止执行者顺手加灯、加形变、改相机 |
| **1. 命名规范** | 物体名、材质槽名 | 跨软件交付时材质槽名错位是重灾区 |
| **2. 分阶段步骤** | 参数表 + **参数意图说明** | 只给值执行者不敢偏离；说明区间和作用才能自适应 |
| **3. 逐阶段验收** | 可数值判定的门槛 | 唯一可靠的反馈通道 |
| **4. 明确禁止** | 反模式清单 | 比正面指导更能防止翻车 |
| **5. 症状 → 原因对照表** | 出问题时按表定位 | 防止凭感觉乱调参数 |

第 5 段是这套方法的差异点。它把通常只存在于资深美术脑子里的排错知识写了下来，让任何执行者都能按图索骥。

---

## 通用禁止事项

写进每一份规格：

1. 在验收门槛未通过时进入下一阶段。
2. 因为中间状态"看起来变差了"而回退上一阶段的正确改动（例：关掉太阳后画面变暗是**正确的中间状态**）。
3. 静默替换方案里指定的方法。**遇到不可用要报告，不要猜替代方案。**
4. 跨软件交付时改动材质槽名或顺序。
5. 交付未经导入验证的资产（尺寸、轴向、原点三项必测）。
6. 为"更真实"而添加与目标风格冲突的元素（磨损、污渍、杂物）——除非规格明确允许。

---

## 参考文件索引

按任务查阅，不要全部载入：

| 文件 | 何时读 |
|---|---|
| `references/gates.md` | **写任何方案时都读。** 各领域的数值门槛库 |
| `references/cloth-sim.md` | 抱枕、床品、软包、任何"充气式"软体 |
| `references/fur-and-velvet.md` | 地毯、绒面、毛发、天鹅绒材质 |
| `references/lighting-ue.md` | UE 照明重构、色调、体积雾 |
| `references/blender-to-ue.md` | 跨软件交付、导出、Reimport、Groom 限制 |
| `references/realism-pass.md` | 场景已完成、需要提升真实感的优化轮 |
| `assets/spec-template.md` | 输出规格文档时套用 |

## 跨模型兼容说明

本 Skill 不依赖任何特定模型或工具链的专有能力。执行者需要具备的能力仅为：

- 能读取本 Skill 的 markdown 文件
- 能通过某种方式驱动目标 DCC 工具（MCP、脚本、插件皆可）
- 能回报执行结果

规格里描述的是**能力需求**而非具体工具名。若执行环境缺少某项能力（例如无法渲染预览来做红底测试），**报告缺失并提议替代验证方式**，不要跳过验收。
